node_modules
.next
.turbo
dist
build
coverage
pnpm-lock.yaml
supabase/migrations
packages/domain/src/generated
*.md
next-env.d.ts

# Comptes rendus engendrés à côté des corpus dérivés : leur forme est décidée
# par le script qui les écrit. Les reformater ferait diverger le fichier de sa
# sortie à chaque régénération, et le contrôle de format échouerait sur une
# différence que personne n'a introduite.
data/**/*.json

# Verrou conda engendré par conda-lock : sa forme est celle de l'outil, et il
# se régénère entier. Le reformater ferait échouer le contrôle à la prochaine
# régénération.
services/geo-worker/conda-lock.yml
